// canonical implementation now lives in components/ui/Avatar - re-exported here so
// existing imports keep working until each caller is migrated in a later milestone
export { default } from "../ui/Avatar";
