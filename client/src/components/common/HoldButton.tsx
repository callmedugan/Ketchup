// canonical implementation now lives in components/ui/HoldButton - re-exported here so
// existing imports keep working until each caller is migrated in a later milestone
export { default } from "../ui/HoldButton";
